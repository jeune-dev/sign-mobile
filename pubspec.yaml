name: sign_application
description: "SIGN — Application de gestion et signature électronique de contrats pour professionnels et particuliers au Sénégal."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1
# IMP-07 : versionName = 1.0.0, versionCode = 1 (premier upload Play Store)
# Pour une mise à jour : incrémenter versionCode ET versionName
# Exemple v1.1.0 : version: 1.1.0+2

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  get_it: ^8.2.0
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  data_connection_checker_tv: ^0.3.5-nullsafety
  dots_indicator: ^4.0.1
  google_fonts: ^6.3.0
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  dartz: ^0.10.1
  flutter_svg: ^2.2.0
  image_picker: ^1.0.7
  intl_phone_field: ^3.2.0
  awesome_dialog: ^3.1.1
  toastification: ^3.0.3
  # IMP-02 : Crash reporting production
  # GP-03 : Firebase v4.x nécessite des dépendances non encore compatibles (Flutter 3.35.6)
  # → Rester sur 3.x stable jusqu'à migration complète de l'écosystème
  firebase_core: ^3.6.0
  firebase_crashlytics: ^4.1.0
  jwt_decode: ^0.3.1
  url_launcher: ^6.1.14
  signature: ^5.4.0
  flutter_pdfview: ^1.3.2
  path_provider: ^2.1.2
  http: ^1.2.0
  permission_handler: ^11.3.1
  dio: ^5.8.0+1
  open_file: ^3.5.10
  media_store_plus: ^0.1.3



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^5.0.0
  # VULN-L03 : Tests de sécurité
  mockito: ^5.4.4
  build_runner: ^2.4.9
  # IMP-08 : flutter_dotenv en dev uniquement (prod utilise --dart-define)
  flutter_dotenv: ^5.1.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    # REST-C01 : .env retiré des assets — ne plus bundler dans l'APK
    # Variables injectées via --dart-define à la compilation
    - assets/icons/
    - assets/fonts/
  # IMP-06 : ACTION MANUELLE REQUISE pour bundler les polices localement :
  # 1. Télécharger depuis https://fonts.google.com/specimen/Plus+Jakarta+Sans
  #    → Télécharger → Extraire les .ttf dans assets/fonts/
  # 2. Décommenter la section ci-dessous et la section assets/fonts/
  # 3. Dans main.dart : GoogleFonts.config.allowRuntimeFetching = false; est déjà positionné
  #
  fonts:
     - family: PlusJakartaSans
       fonts:
         - asset: assets/fonts/PlusJakartaSans-Regular.ttf
         - asset: assets/fonts/PlusJakartaSans-Medium.ttf
           weight: 500
         - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
           weight: 600
         - asset: assets/fonts/PlusJakartaSans-Bold.ttf
           weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_icons:
  android: true
  ios: true
  # IMP-03 : Utilise le logo de meilleure résolution disponible.
  # ACTION MANUELLE REQUISE : Remplacer par un PNG 1024×1024 transparent
  # nommé assets/images/app_icon.png pour une qualité optimale.
  image_path: "assets/images/logo_sign.jpeg"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/logo_sign.jpeg"