# Clé du compte de service Google Play : JAMAIS dans le dépôt. Le workflow
# GitHub l'écrit dans un fichier temporaire et en donne le chemin par
# SUPPLY_JSON_KEY.
json_key_file(ENV["SUPPLY_JSON_KEY"])
package_name("com.signapp.sign_application")
